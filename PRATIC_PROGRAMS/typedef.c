//typedef in c;

// #include <stdio.h>
/*
typedef int susan;
susan main()
{
    susan num1;

    printf("Ënter your number :");
    scanf("%d", &num1);

    num1 = num1 * num1;
    printf("The sqare of the number is %d", num1);
}//end of program

//typedef in array

    typedef int array[5];

    array x = {1, 2, 3, 4, 5};
    printf("Array Elements are:");
    for (int i = 0; i < 5; i++)
    {
        printf("%d\n", x[i]);
    }


//typedef declarations in user defined data type
struct Emp
{
    int eno;
};
void main()
{
    typedef struct Emp employe;
    employe e;
    e.eno = 50;
    printf("%d", e.eno);
}
//typedef in string

typedef char *string;
string read(void);
void main()
{
    string name;
    name = read();
    printf("Wealcome: %s Have a nice day:", name);
}
string read()
{
    string name;
    printf("Enter name : ");
    gets(name);
    return name;
}
//tpedef in memoryallocation
struct node
{
    int data;
    struct node *link;
}
void main()
{
typedef struct node node;
typedef struct node * ptr;
ptr root;
root=(ptr)malloc(sizeof(node));
}
*/
